import { Command } from 'commander';
import { AbstractCommand } from '../abstract/abstract.command';
export declare class FirstMajorReleaseCommand extends AbstractCommand {
    load(program: Command): Command;
}
